{
  "mcpServers": {
    "fixture": {
      "command": "npx",
      "args": ["-y", "@fixture/mcp@latest", "server", "start"],
      "env": {
        "FIXTURE_TOKEN": "${FIXTURE_TOKEN}"
      }
    }
  }
}
